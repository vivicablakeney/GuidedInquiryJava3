function sharedPizza(total,people) {
    var slices = total/people
    console.log(slices)
    // need to assign a variable for final msg
    // ex. Each person gets {slices} of pizza; from our {total slices} of pizza
    // console.log the above message
  }
  sharedPizza(8,2)